import { OutputNamePipe } from './output-name.pipe';

describe('OutputNamePipe', () => {
  it('create an instance', () => {
    const pipe = new OutputNamePipe();
    expect(pipe).toBeTruthy();
  });
});
